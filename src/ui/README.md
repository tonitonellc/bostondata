# BostonData.info Frontend

## Installing dependencies

From `<repo_root>/src/ui/` run `npm install`

## Running locally (dev mode)

From `<repo_root>/src/ui/` run `npm run serve`

## Building static assets

From `<repo_root>/src/ui/` run `npm run build`
